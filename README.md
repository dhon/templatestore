Simple store for a template-selling website.

Clone, npm install, npm start - runs on http://localhost:8000

Created using GitHub: `angular-seed` — the seed for AngularJS apps
https://github.com/angular/angular-seed